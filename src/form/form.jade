each input, i in inputs
  .form-field._icon
    input(type="#{input.type}" placeholder="#{input.placeholder}" data-range="#{input.attr}" required)
.form-field
  button(type="#{submit.type}" value="#{submit.title}") #{submit.title}